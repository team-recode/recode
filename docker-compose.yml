services:
  db:
    image: postgres:16
    container_name: recode-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: recode
      POSTGRES_DB: recode
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      retries: 5

volumes:
  pgdata: